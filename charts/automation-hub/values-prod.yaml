replicaCount: 2

image:
  repository: automation-hub
  tag: stable
  pullPolicy: IfNotPresent

type: ClusterIP
port: 8000

env:
  DATABASE_PATH: /data/automation_hub.sqlite3

resources:
  requests:
    memory: "256Mi"
    cpu: "200m"
  limits:
    memory: "512Mi"
    cpu: "1000m"