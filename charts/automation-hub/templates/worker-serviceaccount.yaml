apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "automation-hub.fullname" . }}-worker-sa
  namespace: {{ .Release.Namespace | default "automation-hub" }}
  labels:
    {{- include "automation-hub.labels" . | nindent 4 }}